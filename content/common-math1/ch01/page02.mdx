---
id: "ch01-sec01-p02"
type: "concept"
title: "[10공수1-01-02] 항등식의 성질과 나머지정리를 이해하고, 이를 활용하여 문제를 해결할 수 있다."
tags: ["다항식", "항등식", "미정계수", "계수비교", "나머지정리", "조립제법"]
---

# [10공수1-01-02] 항등식의 성질과 나머지정리를 이해하고, 이를 활용하여 문제를 해결할 수 있다.

항등식의 성질로 미정계수를 구하고, 나머지 정리로 나눗셈 결과를 빠르게 구하는 것이 이 단원의 목표이다. 앞에서 배운 다항식의 나눗셈·조립제법이 나머지 정리에서 다시 활용된다.

## 1. 항등식

### 1) 항등식의 뜻

<Callout type="definition">
**항등식** — 문자를 포함한 등식에서, 문자에 **어떤 값을 대입해도 항상** 성립하는 등식
</Callout>

문자를 포함한 등식에서, 문자에 어떤 값을 대입하여도 항상 성립하는 등식을 그 문자에 대한 **항등식**이라고 한다.

예를 들어

$$
(x+1)^2=x^2+2x+1
$$

은 $$x$$에 어떤 값을 대입해도 항상 참이다. 따라서 이 등식은 $$x$$에 대한 항등식이다.

반면에

$$
x+3=7
$$

은 $$x=4$$일 때만 참이고, $$x$$에 아무 값이나 대입해도 항상 참인 것은 아니다. 따라서 이 등식은 항등식이 아니다.

<Callout type="tip">
항등식은 **특정한 값에서만** 성립하는 등식이 아니라, **모든 값에서 항상** 성립하는 등식입니다.
</Callout>

<Problem
  id="cmm1-ch01-p05-q01"
  type="truefalse"
  question="$(x+2)(x-2) = x^2 - 4$ 는 $x$에 대한 항등식이다."
  answer={true}
  hints={[
    "합과 차의 곱 공식입니다.",
    "$x$에 어떤 값을 넣어도 양변이 같습니다.",
  ]}
  tags={["항등식"]}
/>

***

### 2) 항등식과 방정식의 차이

항등식과 방정식은 모두 등식이지만 성립하는 방식이 다르다.

<Table
  headers={["구분", "성립 조건", "예시"]}
  data={[
    ["방정식", "특정한 값에서만 성립", "$x + 2 = 5$ ($x = 3$일 때만 참)"],
    ["항등식", "모든 값에서 항상 성립", "$x + 2 = x + 2$, $(x+2)(x-2) = x^2 - 4$"],
  ]}
  caption="표 1. 항등식과 방정식 비교"
/>

$$
x+2=5
$$

는 $$x=3$$일 때만 성립한다. 따라서 이것은 **방정식**이다.

$$
x+2=x+2
$$

는 $$x$$에 어떤 값을 넣어도 항상 성립한다. 따라서 이것은 **항등식**이다.

<Problem
  id="cmm1-ch01-p05-q02"
  type="mcq"
  question="다음 중 $x$에 대한 항등식인 것은?"
  options={["$2x + 1 = 7$", "$x^2 = x^2$", "$x + 1 = 3$", "$3x = 9$"]}
  answer={1}
  hints={[
    "항등식은 $x$에 어떤 값을 넣어도 항상 참입니다.",
    "$x^2 = x^2$ 는 모든 $x$에서 성립합니다.",
  ]}
  tags={["항등식", "방정식"]}
/>

***

### 3) 항등식의 성질

<Callout type="formula">
항등식에서는 **양변의 같은 차수 항의 계수가 각각 같다**.
</Callout>

### 1) $ax^2+bx+c=0$이 항등식인 경우

등식

$$
ax^2+bx+c=0
$$

이 $$x$$에 대한 항등식이라면, 좌변의 다항식이 항상 0이 되어야 하므로

$$
a=0,\quad b=0,\quad c=0
$$

이다.

### 2) 두 다항식이 항등식으로 같은 경우

$$
ax^2+bx+c=a'x^2+b'x+c'
$$

이 $$x$$에 대한 항등식이면 양변의 동류항 계수가 각각 같다.

$$
a=a',\quad b=b',\quad c=c'
$$

예를 들어

$$
2x^2+3x+1=ax^2+bx+c
$$

가 항등식이면 $a=2$, $b=3$, $c=1$이다.

<Table
  headers={["항등식 형태", "결과"]}
  data={[
    ["$ax^2 + bx + c = 0$", "$a = 0$, $b = 0$, $c = 0$"],
    ["$ax^2 + bx + c = a'x^2 + b'x + c'$", "$a = a'$, $b = b'$, $c = c'$"],
  ]}
  caption="표 2. 항등식의 계수 조건"
/>

***

### 4) 계수 비교

항등식에서는 양변의 같은 차수의 항의 계수가 각각 같다. 이 성질을 이용하여 미지의 계수를 구할 수 있다. 이를 **계수 비교**라고 한다.

예를 들어 다음 등식이 $$x$$에 대한 항등식이 되도록 상수 $$a,b,c$$의 값을 구해 보자.

$$
x^2+3x+2=a(x^2-x)+b(x-1)+c
$$

<StepByStep
  title="계수 비교 — $x^2+3x+2 = a(x^2-x)+b(x-1)+c$"
  steps={[
    {
      step: "1단계 — 우변 전개",
      content: "$a(x^2-x)+b(x-1)+c = ax^2 - ax + bx - b + c$\n$= ax^2 + (-a+b)x + (-b+c)$",
    },
    {
      step: "2단계 — 동류항 계수 비교",
      content: "$x^2$항: $a = 1$\n$x$항: $-a + b = 3$\n상수항: $-b + c = 2$",
    },
    {
      step: "3단계 — 순서대로 풀기",
      content: "$a = 1$ → $-1 + b = 3$ → $b = 4$\n$b = 4$ → $-4 + c = 2$ → $c = 6$",
    },
    {
      step: "4단계 — 답",
      content: "$a = 1$, $b = 4$, $c = 6$",
    },
  ]}
/>

***

### 5) 미정계수법

<Callout type="definition">
**미정계수법** — 항등식의 성질을 이용하여 정해져 있지 않은 계수를 구하는 방법
</Callout>

미정계수법에는 대표적으로 두 가지 방법이 있다.

1. 양변을 전개하여 **동류항의 계수를 비교**하는 방법
2. 문자에 **적당한 값을 대입**하여 계수를 구하는 방법

두 방법은 모두 항등식이 모든 값에서 항상 성립한다는 사실을 이용한다.

<Table
  headers={["방법", "핵심 아이디어"]}
  data={[
    ["계수 비교법", "양변을 전개한 뒤 동류항 계수를 비교"],
    ["값 대입법", "계산이 쉬워지는 $x$ 값을 골라 대입"],
  ]}
  caption="표 3. 미정계수법의 두 가지 방법"
/>

***

### 6) 미정계수법 1: 계수 비교 방법

<StepByStep
  title="계수 비교법 — $2x^2+7x+a = (bx+5)(x+c)$"
  steps={[
    {
      step: "1단계 — 우변 전개",
      content: "$(bx+5)(x+c) = bx^2 + bcx + 5x + 5c$\n$= bx^2 + (bc+5)x + 5c$",
    },
    {
      step: "2단계 — 계수 비교",
      content: "$x^2$항: $b = 2$\n$x$항: $bc + 5 = 7$\n상수항: $a = 5c$",
    },
    {
      step: "3단계 — 순서대로 풀기",
      content: "$b = 2$ → $2c + 5 = 7$ → $c = 1$\n$c = 1$ → $a = 5 \\times 1 = 5$",
    },
    {
      step: "4단계 — 답",
      content: "$a = 5$, $b = 2$, $c = 1$",
    },
  ]}
/>

계수 비교 방법은 다음 순서로 사용한다.

1. 양변을 전개한다.
2. 같은 문자, 같은 차수의 항끼리 정리한다.
3. 양변의 동류항 계수를 비교한다.
4. 만들어진 식을 풀어 미정계수를 구한다.

***

### 7) 미정계수법 2: 특정 값 대입 방법

<Callout type="tip">
항등식은 **모든 $x$**에서 성립하므로, 계산이 쉬워지는 값을 골라 대입하면 미정계수를 빠르게 구할 수 있습니다.
$(x-1)$이 있으면 $x = 1$을 대입하면 해당 항이 $0$이 됩니다.
</Callout>

예를 들어

$$
x^2+3x+2=ax(x-1)+b(x-1)+c
$$

<StepByStep
  title="값 대입법 — $x^2+3x+2 = ax(x-1)+b(x-1)+c$"
  steps={[
    {
      step: "1단계 — $x = 1$ 대입 ($(x-1)$ 항을 $0$으로)",
      content: "좌변: $1 + 3 + 2 = 6$\n우변: $a \\cdot 1 \\cdot 0 + b \\cdot 0 + c = c$\n→ $c = 6$",
    },
    {
      step: "2단계 — $x = 0$ 대입",
      content: "좌변: $2$\n우변: $-b + c$\n$c = 6$ → $2 = -b + 6$ → $b = 4$",
    },
    {
      step: "3단계 — $x = -1$ 대입",
      content: "좌변: $1 - 3 + 2 = 0$\n우변: $2a - 2b + c$\n$0 = 2a - 8 + 6$ → $a = 1$",
    },
    {
      step: "4단계 — 답",
      content: "$a = 1$, $b = 4$, $c = 6$",
    },
  ]}
/>

***

### 8) 계수 비교 방법과 값 대입 방법의 선택

<Table
  headers={["방법", "편리한 경우", "예시"]}
  data={[
    ["계수 비교", "전개 후 동류항이 뚜렷할 때", "$ax^2+bx+c = 2x^2-5x+3$ → 바로 $a=2, b=-5, c=3$"],
    ["값 대입", "$(x-1)$, $(x+2)$ 등 인수가 있을 때", "$x=1$ 대입 시 $(x-1)$ 항이 $0$ → $c$를 바로 구함"],
  ]}
  caption="표 4. 방법 선택 가이드"
/>

<Callout type="info">
두 방법의 답은 항상 같습니다. 식의 형태에 따라 **편한 방법**을 선택하면 됩니다.
</Callout>

***

### 9) 핵심 정리

<Callout type="formula">
**항등식 핵심 요약**
</Callout>

<Table
  headers={["개념", "설명"]}
  data={[
    ["항등식", "모든 값에서 항상 성립하는 등식"],
    ["$ax^2+bx+c=0$ 항등식", "$a=b=c=0$"],
    ["두 다항식 항등식", "동류항 계수가 각각 같음"],
    ["계수 비교법", "전개 → 동류항 계수 비교"],
    ["값 대입법", "계산이 쉬운 $x$ 값을 대입"],
  ]}
  caption="표 5. 핵심 개념 요약"
/>

항등식에서 알아야 할 핵심은 다음과 같다.

* 항등식은 문자에 어떤 값을 대입해도 항상 성립하는 등식이다.
* $$ax^2+bx+c=0$$이 $$x$$에 대한 항등식이면 $$a=0,\ b=0,\ c=0$$ 이다.
* $$ax^2+bx+c=a'x^2+b'x+c'$$이 항등식이면 $$a=a',\ b=b',\ c=c'$$ 이다.
* 항등식에서는 양변의 동류항의 계수를 비교할 수 있다.
* 미정계수법은 항등식의 성질을 이용하여 정해지지 않은 계수를 구하는 방법이다.

따라서 항등식은 단순히 “항상 참인 등식”이라는 뜻을 넘어서, 다항식의 계수를 비교하고 미지의 계수를 구하는 데 중요한 도구가 된다.

***

### 연습 문제

<Problem
  id="cmm1-ch01-p05-q03"
  type="mcq"
  question="$ax^2 + bx + c = 0$ 이 $x$에 대한 항등식이면 $a, b, c$의 값은?"
  options={["$a=1, b=1, c=1$", "$a=0, b=0, c=0$", "$a=0, b=0, c$는 임의", "$a, b, c$ 모두 임의"]}
  answer={1}
  hints={[
    "항등식이면 좌변이 항상 0이어야 합니다.",
    "모든 차수의 계수가 0이어야 합니다.",
  ]}
  tags={["항등식", "성질"]}
  solution={[
    { step: "항등식 조건", content: "$a = 0$, $b = 0$, $c = 0$" },
  ]}
/>

<Problem
  id="cmm1-ch01-p05-q04"
  type="numerical"
  question="$2x^2 + 5x + 3 = ax^2 + bx + c$ 가 항등식일 때 $a + b + c$ 의 값은? (정수만 입력)"
  answer={10}
  hints={[
    "동류항 계수를 비교합니다.",
    "$a = 2$, $b = 5$, $c = 3$",
  ]}
  tags={["계수비교"]}
  solution={[
    { step: "계수 비교", content: "$a = 2$, $b = 5$, $c = 3$" },
    { step: "답", content: "$2 + 5 + 3 = 10$" },
  ]}
/>

<Problem
  id="cmm1-ch01-p05-q05"
  type="order"
  question="계수 비교법으로 미정계수를 구하는 과정을 순서대로 배열하시오."
  items={[
    "양변을 전개한다",
    "같은 차수의 항끼리 정리한다",
    "양변의 동류항 계수를 비교한다",
    "연립하여 미정계수를 구한다",
  ]}
  tags={["미정계수법", "계수비교"]}
/>

<Problem
  id="cmm1-ch01-p05-q06"
  type="truefalse"
  question="$x^2 + 3x + 2 = a(x-1) + b$ 가 항등식일 때, $x = 1$을 대입하면 $b$를 바로 구할 수 있다."
  answer={true}
  hints={[
    "$x = 1$을 대입하면 $(x-1) = 0$이 됩니다.",
    "우변의 $a(x-1)$ 항이 사라져 $b$만 남습니다.",
  ]}
  tags={["값대입법"]}
/>

***

항등식에서는 **임의의 값**을 대입해도 등식이 성립한다. 나머지 정리도 같은 아이디어에서 출발한다. 다항식 $P(x)$를 $x-a$로 나눌 때의 나머지는, 나눗셈 전체를 하지 않아도 $P(a)$만 계산하면 구할 수 있다. 이 결과는 조립제법으로 구한 나머지와 일치하며, 인수 정리의 기반이 된다.

## 2. 나머지 정리

### 1) 나머지 정리의 뜻

<Callout type="definition">
**나머지 정리** — 다항식 $P(x)$를 일차식 $x-a$로 나누었을 때의 나머지는, 나눗셈 없이 $P(a)$로 구할 수 있다.
</Callout>

다항식 $$P(x)$$를 일차식 $$x-a$$로 나누었을 때의 나머지는, 직접 나눗셈을 하지 않아도 $$P(x)$$에 $$x=a$$를 대입하여 구할 수 있다. 앞에서 배운 조립제법으로 구한 나머지와 항상 같은 값이다.

<Callout type="formula">
$$
P(x) \text{를 } x-a \text{로 나눈 나머지} = P(a)
$$
</Callout>

예를 들어 $$P(x)$$를 $$x-3$$으로 나눈 나머지는 $$P(3)$$이고, $$P(x)$$를 $$x+2$$로 나눈 나머지는 $$P(-2)$$이다.

<Table
  headers={["나누는 식", "$a$ 값", "나머지"]}
  data={[
    ["$x - 3$", "$3$", "$P(3)$"],
    ["$x + 2$", "$-2$", "$P(-2)$"],
    ["$x - 2$", "$2$", "$P(2)$"],
  ]}
  caption="표 6. 나머지 정리 적용 예시"
/>

<Problem
  id="cmm1-ch01-p06-q01"
  type="truefalse"
  question="$P(x)$ 를 $x - 5$ 로 나눈 나머지는 $P(5)$ 이다."
  answer={true}
  hints={[
    "나머지 정리: $x - a$로 나눌 때 나머지는 $P(a)$",
    "$a = 5$",
  ]}
  tags={["나머지정리"]}
/>

***

### 2) 나머지 정리가 성립하는 이유

<StepByStep
  title="나머지 정리 증명"
  steps={[
    {
      step: "1단계 — 나눗셈 구조",
      content: "$P(x) = (x-a)Q(x) + R$ ($R$은 상수, $x-a$가 일차식이므로)",
    },
    {
      step: "2단계 — $x = a$ 대입",
      content: "항등식이므로 $x = a$를 대입: $P(a) = (a-a)Q(a) + R$",
    },
    {
      step: "3단계 — 정리",
      content: "$a - a = 0$ 이므로 $P(a) = R$",
    },
    {
      step: "결론",
      content: "나머지 $R = P(a)$",
    },
  ]}
/>

즉, $$P(x)=(x-a)Q(x)+R$$ 에서 $$x=a$$를 대입하면 $$P(a)=R$$ 이다.

***

### 3) 나머지 정리의 핵심 공식

<Callout type="formula">
$$
R = P(a) \quad \left( P(x) \text{를 } x-a \text{로 나눌 때} \right)
$$
</Callout>

<Table
  headers={["주의 사항", "설명"]}
  data={[
    ["나누는 식", "$x - a$ 꼴이어야 한다"],
    ["$a$ 찾기", "$x - a = 0$ 에서 $a$를 구한다"],
    ["대입", "$P(a)$가 바로 나머지"],
    ["나머지 형태", "일차식으로 나누므로 나머지는 항상 상수"],
  ]}
  caption="표 7. 나머지 정리 사용 시 주의"
/>

***

### 4) 나머지 빠르게 구하기

나머지 정리를 이용하면 다항식의 나눗셈을 직접 하지 않고도 나머지를 빠르게 구할 수 있다.

예를 들어

$$
P(x)=x^3-2x^2+3x-5
$$

를 $$x-2$$로 나누었을 때의 나머지를 구해 보자.

<StepByStep
  title="$P(x) = x^3 - 2x^2 + 3x - 5$ 를 $x - 2$ 로 나눈 나머지"
  steps={[
    {
      step: "1단계 — $a$ 확인",
      content: "나누는 식 $x - 2$ → $a = 2$",
    },
    {
      step: "2단계 — $P(2)$ 계산",
      content: "$P(2) = 2^3 - 2 \\cdot 2^2 + 3 \\cdot 2 - 5$\n$= 8 - 8 + 6 - 5$",
    },
    {
      step: "3단계 — 결과",
      content: "나머지 $= P(2) = 1$",
    },
  ]}
/>

<Callout type="tip">
조립제법이나 긴 나눗셈 대신 **한 번의 대입**으로 나머지를 구할 수 있습니다.
</Callout>

***

### 5) $x+a$로 나눌 때

<Callout type="info">
나누는 식이 $x + a$ 꼴이면 $x + a = x - (-a)$ 로 생각하여 $a$ 대신 $-a$를 대입합니다.
</Callout>

예를 들어 $$P(x)=2x^2+x-4$$ 를 $$x+3$$으로 나누었을 때:

$$
x+3 = x-(-3) \quad \Rightarrow \quad a = -3
$$

나머지는 $$P(-3)$$ 이다.

<StepByStep
  title="$P(x) = 2x^2 + x - 4$ 를 $x + 3$ 으로 나눈 나머지"
  steps={[
    {
      step: "1단계 — $a$ 확인",
      content: "$x + 3 = x - (-3)$ → $a = -3$",
    },
    {
      step: "2단계 — $P(-3)$ 계산",
      content: "$P(-3) = 2(-3)^2 + (-3) - 4 = 18 - 3 - 4 = 11$",
    },
    {
      step: "3단계 — 결과",
      content: "나머지 $= 11$",
    },
  ]}
/>

<Problem
  id="cmm1-ch01-p06-q02"
  type="mcq"
  question="$P(x)$ 를 $x + 1$ 로 나눈 나머지를 구하려면?"
  options={["$P(1)$", "$P(-1)$", "$P(0)$", "$P(2)$"]}
  answer={1}
  hints={[
    "$x + 1 = x - (-1)$",
    "$a = -1$ → $P(-1)$",
  ]}
  tags={["나머지정리"]}
  solution={[
    { step: "꼴 변환", content: "$x + 1 = x - (-1)$, $a = -1$" },
    { step: "답", content: "나머지 $= P(-1)$" },
  ]}
/>

***

### 6) $ax+b$로 나눌 때의 나머지

<Callout type="definition">
$P(x)$를 $ax + b$ ($a \neq 0$)로 나눌 때, $ax + b = 0$ 에서 $x = -\frac{b}{a}$ 를 대입한다.

$$
\text{나머지} = P\left(-\frac{b}{a}\right)
$$
</Callout>

예를 들어 $$P(x)$$를 $$2x-1$$로 나눌 때:

$$
2x - 1 = 0 \quad \Rightarrow \quad x = \frac12
$$

나머지는 $$P\left(\frac12\right)$$ 이다.

<Table
  headers={["나누는 식", "대입할 $x$ 값"]}
  data={[
    ["$x - a$", "$a$"],
    ["$x + a$", "$-a$"],
    ["$ax + b$", "$-\\frac{b}{a}$"],
  ]}
  caption="표 8. 나누는 식에 따른 대입 값"
/>

***

### 7) 조건에서 식 만들기

나머지 정리는 미지 계수를 구하는 데에도 사용된다.

예를 들어

$$
P(x)=x^3+ax^2-3x+1
$$

을 $$x-2$$로 나누었을 때의 나머지가 $$5$$라고 하자.

<StepByStep
  title="미지수 $a$ 구하기"
  steps={[
    {
      step: "1단계 — 나머지 정리 적용",
      content: "$P(2) = 5$",
    },
    {
      step: "2단계 — $P(2)$ 계산",
      content: "$P(2) = 8 + 4a - 6 + 1 = 4a + 3$",
    },
    {
      step: "3단계 — 방정식 풀기",
      content: "$4a + 3 = 5$ → $4a = 2$ → $a = \\frac{1}{2}$",
    },
  ]}
/>

<Callout type="tip">
"나머지가 $k$이다" → $P(a) = k$ 라는 식으로 바꿉니다.
</Callout>

***

### 8) 두 조건이 주어진 경우

나머지 정리는 조건이 두 개 이상 주어진 문제에서도 사용할 수 있다.

예: $$P(x)$$를 $$x-1$$로 나누면 나머지 $$2$$, $$x-3$$으로 나누면 나머지 $$8$$.

$$(x-1)(x-3)$$으로 나눌 때 나머지는 일차식 $$ax+b$$ 로 놓을 수 있다.

<StepByStep
  title="$(x-1)(x-3)$ 으로 나눌 때 나머지 구하기"
  steps={[
    {
      step: "1단계 — 나머지 정리로 조건 세우기",
      content: "$P(1) = 2$, $P(3) = 8$\n$P(x) = (x-1)(x-3)Q(x) + ax + b$",
    },
    {
      step: "2단계 — $x = 1, 3$ 대입",
      content: "$x = 1$: $a + b = 2$\n$x = 3$: $3a + b = 8$",
    },
    {
      step: "3단계 — 연립",
      content: "두 식을 빼면 $2a = 6$ → $a = 3$\n$3 + b = 2$ → $b = -1$",
    },
    {
      step: "4단계 — 결과",
      content: "나머지 $= 3x - 1$",
    },
  ]}
/>

***

### 9) 나머지 정리와 나누어떨어짐

<Callout type="warning">
$P(a) = 0$ 이면 나머지가 $0$ → $P(x)$는 $x - a$로 **나누어떨어진다**.

즉, $x - a$는 $P(x)$의 **인수**이다. (다음 절 **인수정리**와 연결)
</Callout>

$$
P(a) = 0 \quad \Leftrightarrow \quad P(x) \text{는 } x - a \text{로 나누어떨어진다}
$$

***

### 10) 핵심 정리

<Callout type="formula">
**나머지 정리 핵심 요약**
</Callout>

<Table
  headers={["상황", "나머지"]}
  data={[
    ["$x - a$ 로 나눌 때", "$P(a)$"],
    ["$x + a$ 로 나눌 때", "$P(-a)$"],
    ["$ax + b$ 로 나눌 때", "$P\\left(-\\frac{b}{a}\\right)$"],
    ["나머지가 $k$", "$P(a) = k$ 로 방정식 세우기"],
    ["$P(a) = 0$", "$x - a$로 나누어떨어짐 (인수)"],
  ]}
  caption="표 9. 핵심 정리 모음"
/>

나머지 정리에서 알아야 할 핵심은 다음과 같다.

* 다항식 $$P(x)$$를 $$x-a$$로 나누었을 때의 나머지는 $$P(a)$$이다.
* 나누는 식이 $$x+a$$일 때에는 $$-a$$를 대입한다.
* 나누는 식이 $$ax+b$$일 때에는 $$-\frac{b}{a}$$를 대입한다.
* 나머지 정리를 이용하면 직접 나눗셈을 하지 않고도 나머지를 구할 수 있다.
* “나머지가 얼마이다”라는 조건은 $$P(a)=\text{나머지}$$라는 식으로 바꿀 수 있다.
* $$P(a)=0$$이면 $$P(x)$$는 $$x-a$$로 나누어떨어진다.

따라서 나머지 정리는 다항식의 나눗셈을 직접 계산하지 않고, **대입만으로 나머지를 구하게 해 주는 중요한 정리**이다.

***

### 연습 문제

<Problem
  id="cmm1-ch01-p06-q03"
  type="numerical"
  question="$P(x) = x^2 - 3x + 2$ 를 $x - 2$ 로 나눈 나머지는? (정수만 입력)"
  answer={0}
  hints={[
    "나머지 $= P(2)$",
    "$P(2) = 4 - 6 + 2 = 0$",
  ]}
  tags={["나머지정리"]}
  solution={[
    { step: "대입", content: "$P(2) = 4 - 6 + 2 = 0$" },
  ]}
/>

<Problem
  id="cmm1-ch01-p06-q04"
  type="mcq"
  question="$P(x) = x^3 + 2x - 1$ 을 $x - 1$ 로 나눈 나머지는?"
  options={["$0$", "$1$", "$2$", "$-1$"]}
  answer={2}
  hints={[
    "나머지 $= P(1)$",
    "$P(1) = 1 + 2 - 1 = 2$",
  ]}
  tags={["나머지정리"]}
  solution={[
    { step: "대입", content: "$P(1) = 1 + 2 - 1 = 2$" },
  ]}
/>

<Problem
  id="cmm1-ch01-p06-q05"
  type="truefalse"
  question="$P(3) = 0$ 이면 $P(x)$ 는 $x - 3$ 으로 나누어떨어진다."
  answer={true}
  hints={[
    "나머지 정리에 의해 나머지 $= P(3) = 0$",
    "나머지가 0이면 나누어떨어집니다.",
  ]}
  tags={["나누어떨어짐", "인수정리"]}
/>

<Problem
  id="cmm1-ch01-p06-q06"
  type="order"
  question="나머지 정리로 $P(x)$를 $x - a$로 나눈 나머지를 구하는 과정을 순서대로 배열하시오."
  items={[
    "나누는 식 $x - a$ 에서 $a$를 확인한다",
    "$P(x)$에 $x = a$를 대입하여 $P(a)$를 계산한다",
    "$P(a)$가 나머지이다",
  ]}
  tags={["나머지정리"]}
/>
